<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.handclassapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.AddStuInforActivity">

        </activity>
        <activity android:name=".Activity.adminStuAdd" />
        <activity android:name=".Activity.adminStuQuery" >
            <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter></activity>>
        <activity android:name=".Activity.TeacherActivity" />
        <activity android:name=".Activity.StudentActivity" />
        <activity android:name=".Activity.adminStu" />
        <activity android:name=".Activity.adminTea" />
        <activity android:name=".Activity.AdminActivity">

        </activity>
        <activity android:name=".Activity.LoginActivity"></activity>
    </application>
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.
WRITE_EXTERNAL_STORAGE" />
    <!--请求访问使用照相设备-->
    <uses-permission android:name="android.permission.CAMERA" />

</manifest>